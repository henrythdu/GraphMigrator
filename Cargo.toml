[workspace]
resolver = "2"
members = [
    "crates/cli",
    "crates/core",
]

[workspace.dependencies]
# Shared versions for all crates (as of January 2025)
petgraph = "0.8"
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
